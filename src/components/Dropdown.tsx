import React, { useState } from "react"

interface Props {
	options: { label: string; value: string }[]
}

function Dropdown({ options }: Props) {
	return <div style={{}}>Dropdown</div>
}
export default Dropdown
